Chat application written in Nim.

Serve runs a server which is a singleton and you can run as many clients as you want to connect to said server, just pass host and username.

`client localhost Josh`
